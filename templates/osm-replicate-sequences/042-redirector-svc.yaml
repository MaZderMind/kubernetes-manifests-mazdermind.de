kind: Service
apiVersion: v1
metadata:
  name: redirector
  labels:
    app: redirector
  namespace: osm-replicate-sequences
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
  selector:
    app: redirector
