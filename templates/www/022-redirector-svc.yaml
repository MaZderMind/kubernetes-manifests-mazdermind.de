kind: Service
apiVersion: v1
metadata:
  name: redirector
  labels:
    app: redirector
  namespace: www
spec:
  ports:
    - name: web
      port: 8080
      protocol: TCP
  selector:
    app: redirector
