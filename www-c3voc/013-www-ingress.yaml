apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  labels:
    app: www
  name: www
  namespace: www-c3voc
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
  - host: c3voc.kube.mazdermind.de
    http:
      paths:
      - backend:
          serviceName: www
          servicePort: www
